import React, { useState } from "react";

import Form from "./components/Form/Form";

const App = () => {
  const occupation = useState(null);
  const state = useState(null);

  return (
    <>
      <Form />
    </>
  );
};

export default App;
